
resource "aws_subnet" "pubsub" {
  count = "${data.aws_availability_zones.azs.name}"
  vpc_id     = "${aws_vpc.mvpc.id}"
  cidr_block = "${cidrsubnet(var.vpc_cidr,4,count.index + 1)}"

  tags = {
    Name = "pub-${count.index}"
  }
}
